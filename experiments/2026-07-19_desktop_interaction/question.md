# Desktop interaction retention

Can an earlier, smaller Desktop-web timeline virtualization window materially reduce the p95 time
until the selected title and visible body agree, plus inactive DOM and renderer memory, without
changing stream semantics or the retained behavior required by terminals, browser tabs, and native
panels?
