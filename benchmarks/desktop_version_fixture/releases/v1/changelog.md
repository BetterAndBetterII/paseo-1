# v1

- Freeze the local deterministic H176 eight-tab workload and 1 MiB open TypeScript fence workload.
- Use three complete tab cycles and separate metric/GIF passes.
- Open the 1 MiB Markdown case from a dedicated, previously unvisited workspace so inactive-tab
  pre-rendering cannot warm the renderer before the measured click.
